# Generated by stoatworks-unraid/scripts/gen-docker.mjs from fleet.json.
# Repo: otter-edid-editor. Edit fleet.json and regenerate — direct edits are overwritten.
#
# Pull the published image by default. Uncomment build: . to build from this
# checkout instead — useful when testing a change before it reaches GHCR.

services:
  otter-edid-editor:
    image: ghcr.io/stoatworks-labs/otter-edid-editor:latest
    # build: .
    container_name: otter-edid-editor
    restart: unless-stopped
    ports:
      - "8529:80"
